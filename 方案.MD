# 悬浮文字放大工具（HoverText）实现方案

## 技术栈建议

- C# / .NET 8
- WPF 或 WinUI 3（悬浮窗口）
- Windows UI Automation（读取鼠标下文字）
- Windows.Media.Ocr 或 Tesseract（OCR 兜底）
- Win32 API（全局热键、鼠标位置、置顶透明窗口）
- 可选：Windows Magnification API（像素放大兜底）
  - 官方文档：`Magnification API`  
    https://learn.microsoft.com/en-us/windows/win32/winauto/magapi/magapi-intro

## 工作流

1. 用户按住一个热键（如 `Ctrl` / `Alt`）
2. 程序每 100-200ms 获取鼠标坐标
3. 先用 **UI Automation** 找鼠标下控件与文本
4. 如果能拿到文字：在鼠标附近显示大号悬浮文本框
5. 如果拿不到：截取鼠标周围小区域做 OCR
6. 如果 OCR 也失败：退回像素放大镜

## 实现结构

```text
HoverText.exe
├─ InputService
│  ├─ 全局热键
│  └─ 鼠标位置追踪
├─ TextProbeService
│  ├─ UI Automation 读取文字
│  ├─ ValuePattern / NameProperty 兜底
│  └─ TextPattern.RangeFromPoint 读取鼠标所在文本
├─ OcrService
│  ├─ 截取鼠标周围区域
│  └─ OCR 识别最近一行/一个词
├─ MagnifierFallback
│  └─ 像素放大模式
├─ OverlayWindow
│  ├─ 置顶
│  ├─ 透明背景
│  ├─ 鼠标穿透
│  └─ 高对比大字显示
└─ Settings
   ├─ 字号
   ├─ 颜色
   ├─ 触发键
   ├─ 延迟时间
   └─ OCR 开关
```

## MVP 功能建议

- 按住 `Alt` 显示鼠标下文字
- 支持 Notepad、Edge、Chrome、VS Code、文件资源管理器
- 悬浮框字体 32-48px
- 自动避开屏幕边缘
- 鼠标穿透，不影响点击
- 设置开机自启
- OCR 作为可选开关

## 难点与风险

- Chrome / Edge / Electron 有时 UIA 给出的文本不够精确
- VS Code 等编辑器可能只能拿到整行或控件名
- PDF、图片、游戏、远程桌面场景基本依赖 OCR 或像素放大
- 多显示器 + 不同 DPI 缩放会导致坐标换算复杂
- 悬浮窗不能抢焦点，避免影响调试体验
- 高权限窗口通常要求工具以管理员运行才能稳定读取

## 版本推进建议

我的判断：值得开发，可先做“近视调试专用”小工具，不要一开始追求全系统完美。先覆盖浏览器、IDE、终端、设置窗口、普通 Win32 应用，也已很实用。

### v0.1

- UIA 读取鼠标下文本 + 悬浮大字框

### v0.2

- 支持 Chrome / Edge / VS Code 常见场景优化

### v0.3

- OCR 兜底

### v0.4

- 像素放大兜底

### v0.5

- 托盘菜单、设置界面、开机自启

### v1.0

- 安装包、自动更新、异常日志、多显示器 DPI 修复

